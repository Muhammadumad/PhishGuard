{
  "version": 2,
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "phishguard-frontend/dist",
  "installCommand": "cd phishguard-frontend && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
