# PhishGuard Project Rules

## Git & Repository Directives
- **Auto-Push Policy**: Whenever code modifications, new features, bug fixes, database migrations, or refactoring are completed, stage all changes, create a clear structured commit message, and automatically push them to GitHub (`git push origin main`).
